# Setlist Vote — October Anniversary Show

Single-page voting sheet for the band and the organiser.
Everything lives in `index.html` — no build step, no dependencies.

Live at: `https://<your-username>.github.io/setlist-vote/`

The companion Google Sheet (self-tallying) is linked from the page itself.
